# Create a column called `activity_type` by extracting a substring from `pod`

Create a column called `activity_type` by extracting a substring from
`pod`

## Usage

``` r
get_activity_type_from_pod(tbl)
```

## Arguments

- tbl:

  A tibble
