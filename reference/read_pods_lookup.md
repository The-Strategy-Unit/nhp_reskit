# Read the pods lookup table from a YAML file in the NHP Outputs repo

Read the pods lookup table from a YAML file in the NHP Outputs repo

## Usage

``` r
read_pods_lookup(file = "golem-config.yml")
```

## Arguments

- file:

  The name of the yaml file to be read in
