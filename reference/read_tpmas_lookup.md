# Read the TPMAs lookup table from a CSV file in the TPMAs repo

Read the TPMAs lookup table from a CSV file in the TPMAs repo

## Usage

``` r
read_tpmas_lookup(file = "tpma-lookup.csv")
```

## Arguments

- file:

  The name of the csv file to be read in
