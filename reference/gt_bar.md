# Function to handle the size and formatting of gt_bar elements in tables

Function to handle the size and formatting of gt_bar elements in tables

## Usage

``` r
gt_bar(x, format_fn = NULL, colours = c("#ec6555", "#f9bf07"))
```
