# Prepare a lookup table for all current TPMAs

Prepare a lookup table for all current TPMAs

## Usage

``` r
get_tpma_label_lookup()
```

## Value

A 4-column tibble, with columns `strategy`, `activity_type`,
`change_factor` and `tpma_label`
