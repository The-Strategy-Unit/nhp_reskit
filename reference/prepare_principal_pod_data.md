# Initial preparation of site-level data for the main summary table

Initial preparation of site-level data for the main summary table

## Usage

``` r
prepare_principal_pod_data(default_tbl, pod_lookup)
```

## Arguments

- default_tbl:

  the "default" table from NHP results

## Value

A tibble
