# Get the direct URL to a file in the TPMAs GitHub repo

Get the direct URL to a file in the TPMAs GitHub repo

## Usage

``` r
get_tpmas_gh_url(...)
```

## Arguments

- ...:

  Pass the name of the file in via `...`
