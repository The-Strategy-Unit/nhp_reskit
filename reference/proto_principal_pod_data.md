# Zero-row prototype for the [compile_principal_pod_data](https://the-strategy-unit.github.io/nhp_reskit/reference/compile_principal_pod_data.md) output

The column names and types here must match what
[compile_principal_pod_data](https://the-strategy-unit.github.io/nhp_reskit/reference/compile_principal_pod_data.md)
returns when rows are present; `test-empty_results.R` asserts this.

## Usage

``` r
proto_principal_pod_data()
```

## Value

A zero-row tibble
