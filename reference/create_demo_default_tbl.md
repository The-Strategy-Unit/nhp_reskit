# Function to create a "default" table with synthetic values

The table contains synthetic (randomly created) values for a selection
of PODs and measures, across 2 "sites", as if created by 64 model runs

## Usage

``` r
create_demo_default_tbl(seed)
```

## Arguments

- seed:

  integer, for reproducibility of outputs, a seed value to be used
