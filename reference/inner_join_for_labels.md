# Use a lookup table to get more readable labels for PoDs

Use a lookup table to get more readable labels for PoDs

## Usage

``` r
inner_join_for_labels(tbl, lookup)
```

## Arguments

- tbl:

  A tibble

- lookup:

  A lookup table with pod and pod_label columns
