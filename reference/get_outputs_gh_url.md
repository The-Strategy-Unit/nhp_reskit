# Get the direct URL to a file in the NHP Outputs GitHub repo

Get the direct URL to a file in the NHP Outputs GitHub repo

## Usage

``` r
get_outputs_gh_url(...)
```

## Arguments

- ...:

  Pass the name of the file in via `...`
