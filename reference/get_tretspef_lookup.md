# Get a lookup of tretspef codes to descriptions

Get a lookup of tretspef codes to descriptions

## Usage

``` r
get_tretspef_lookup()
```

## Value

A 2-column tibble with columns `code` and `tretspef`
