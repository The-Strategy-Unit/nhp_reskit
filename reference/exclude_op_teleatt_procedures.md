# Exclude outpatient procedures from tele-attendances count only

Exclude outpatient procedures from tele-attendances count only

## Usage

``` r
exclude_op_teleatt_procedures(tbl)
```

## Arguments

- tbl:

  A tibble
